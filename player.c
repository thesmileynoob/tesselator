#include "object.h"

void player_reset(object* Player, int xpos, int ypos)
{
        // object result = *Player;
        // result.Width  = 150;
        // result.Height = 40;
        // result.Xpos   = xpos;
        // result.Ypos   = ypos - result.Height;
        // result.Type   = PLAYER;
        // result.State  = IDLE;

        // *Player = result;
}
